include "application.conf"

play {
  evolutions {
    db {
      default {
        autoApply = true
        autoApplyDowns = true
      }
    }
  }
}

db {
  default {
    url = "jdbc:postgresql://localhost/shorturl-test"
    url = ${?DATABASE_URL}
  }
}